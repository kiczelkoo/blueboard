import { MyPosition } from './my-position';

export class Operation {
   uuid: string;
   name: string;
   currentPos: MyPosition;
   prevPos: MyPosition;
   lineWidth: number;
   lineColor: any;
   lineCap: any;
   image: string;
   boardNumber: String;
}
